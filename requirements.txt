# 금형 보정시트 자동 생성 데모 — 실행 의존성
# 설치:  pip install -r requirements.txt

# ── 파트 2 (0-Line 검출) 필수 ────────────────────────────────────
numpy>=1.26
opencv-python>=4.9        # 색상 변환, 형태학 연산, 연결요소
pandas>=2.1               # CSV 산출
Pillow>=10.0              # 결과 이미지에 한글 제목 렌더링 (demo.py)

# ── 파트 1 (UI) ─────────────────────────────────────────────────
streamlit>=1.32

# ── 참고 ────────────────────────────────────────────────────────
# 파트 3 (OCR), 파트 4 (인페인팅), 파트 5 (깊이 추정) 의 의존성은
# 각 담당자가 구현하면서 여기에 추가한다.
# 로컬 구동이 가능한 모델만 사용할 것 (보안, 8/12 멘토링 확인 사항).
#
# 예상 후보:
#   paddleocr / easyocr        파트 3
#   torch, transformers        파트 4·5 (로컬 모델)
