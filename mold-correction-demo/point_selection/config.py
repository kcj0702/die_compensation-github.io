"""보정시트에 올릴 주요 편차 포인트를 고르는 기준값."""

# 이웃은 스캔 픽셀 좌표에서 찾는다. 이미지 대비 %는 x와 y의 실제 길이가 달라
# (예: 1306x680 스캔에서 1%는 가로 13px, 세로 6.8px) 등방적이지 않다.

# 피크: 이웃들 사이에서 signed 값이 국소 극값인 지점.
# 이웃 수가 많을수록 더 넓은 범위의 극값만 남는다. 실측에서 5개일 때
# 79개 중 10개, 130개 중 15개, 40개 중 4개가 뽑혔다.
PEAK_NEIGHBOURS = 5
# 0 근처의 미세한 요철이 피크로 잡히는 것을 막는다. 판독값이 0.1mm 단위다.
PEAK_MIN_ABS_MM = 0.3

# 부호 변화: 이웃과 부호가 반대인 지점. 양쪽 모두 이 크기를 넘어야 인정한다.
# 한쪽만 보면 0 근처 진동이 전부 걸려 전체의 절반이 선택된다.
SIGN_NEIGHBOURS = 3
SIGN_MIN_ABS_MM = 0.2
# 제로 크로싱은 점이 아니라 선이라 인접 후보가 무더기로 나온다. 이 거리 안의
# 후보는 하나로 묶고 |편차|가 가장 큰 것만 남긴다. 대각선 대비 비율이다.
SIGN_MERGE_RADIUS_RATIO = 0.06

# 전체 최대·최소는 금형 보정의 기준점이라 항상 남긴다.
KEEP_GLOBAL_EXTREMES = True
