## 보정 시트: 이미지 방향 조작 시 라벨 위치·지시선 길이 고정

### Summary

- **회전/반전해도 라벨이 테두리를 따라가도록 수정**: 정면도 이미지를 90°/180°/270° 회전하거나 좌우·상하 반전해도 보정치 라벨이 이제 이미지와 함께 정확히 같은 위치를 유지합니다. 이전에는 라벨 위치가 고정된 채 이미지만 돌아서 라벨-지시선 배치가 꼬였습니다.
- **회전해도 지시선 길이가 변하지 않도록 수정**: 점→라벨 오프셋을 화면 퍼센트가 아니라 실제 픽셀 기반의 등거리(isometric) 벡터로 저장하고, 회전 시 등방(isotropic) 척도로 정규화해 돌리도록 바꿨습니다. 그 결과 회전마다 미묘하게 달라지던 지시선 길이가 완전히 고정됩니다(8회 연속 회전 테스트에서 라벨-점 거리 값이 소수점까지 정확히 반복되는 것으로 검증).
- **회전 직후 창 크기 레이스 컨디션 수정**: 회전하면 정면도 창(SheetCanvas)이 통째로 리마운트되는데, 이때 부모(ServicePreview)가 새 창의 실제 픽셀 크기를 미처 모른 상태에서 라벨 위치를 미리 계산해 넘기는 타이밍 문제가 있었습니다. 라벨 컴포넌트가 자기 자신이 실측한 최신 크기로 직접 계산하도록 구조를 바꿔 근본적으로 해결했습니다.
- **회전 반복 시 정면도 창이 계속 작아지던 버그 수정**: 90° 회전을 반복하면 창이 점점 작아지던 문제를 넓이 보존(area-preserving) 리핏 공식으로 수정했습니다(가로/세로 비율만 다시 맞추고 넓이는 그대로 유지).
- **다른 품번으로 전환 시 이전 라벨 위치가 새 파트로 새는 버그 수정**: 정면도/Detail 레이아웃, 라벨 위치 상태를 scan.id로 완전히 분리 저장해 파트 전환 시 상태가 섞이지 않도록 했습니다.
- **기본 정면도 창 크기 1.5배 확대**.

### Test plan

- [x] `npx tsc --noEmit` — 기존에 있던 무관한 오류 2건 외 신규 오류 없음
- [x] `python -m unittest tests.test_sheet_export` — 25/25 통과
- [x] 브라우저에서 직접 확인: 포인트 추가 → 라벨 드래그 → 90°/180°/270°/360° 회전 8회 반복 → 점-라벨 중심 거리 측정값이 회전 각도별로 완전히 동일하게 반복(드리프트 없음)
- [x] 파트 전환 시 라벨 위치가 새 파트로 새지 않음을 fiber 상태 직접 확인

🤖 Generated with [Claude Code](https://claude.com/claude-code)
