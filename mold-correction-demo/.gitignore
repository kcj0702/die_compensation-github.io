data/raw/*
data/sample/*
data/intermediate/*
data/output/*

!data/raw/.gitkeep
!data/sample/.gitkeep
!data/intermediate/.gitkeep
!data/output/.gitkeep

__pycache__/
*.py[cod]
.venv/
AJ_ENV/
.pytest_cache/

# Company scan inputs and generated label-removal results
label_removal/input/*
label_removal/output/*

!label_removal/input/.gitkeep
!label_removal/output/.gitkeep

# 파싱한 STEP 캐시 — 파일당 수 MB 라 저장소에 넣지 않는다
cad_import/_parsed/
# 현업 제로라인 파이프라인 결과 (실측 64XX2 117초 -> 다시 안 돈다)
zero_line_detection/.lab_cache/
# 적응형 제로라인 결과 (실측 67XX6 12초 -> 다시 안 돈다)
zero_line_detection/.adaptive_cache/
# Qwen 라벨 판독 결과 (실측 64XX2 57초 -> 다시 안 읽는다)
ui/backend/.label_cache.json
